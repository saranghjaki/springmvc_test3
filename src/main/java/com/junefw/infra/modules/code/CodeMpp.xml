<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.junefw.infra.modules.code.CodeMpp"> <!-- 변경파일지칭 -->

    <resultMap id="resultMapObj" type="com.junefw.infra.modules.code.Code"></resultMap>
   <!-- 결과객체지정 -->
	<select id="selectList" resultMap="resultMapObj">
		SELECT 
			a.pilcgSeq
			,a.pilcgName
			,a.pilcgDelNy
				FROM nct.pilatesCodeGroup a
			WHERE 1=1
				<choose>
					<when test="shPilcgDelNy==1">AND pilcgDelNy=1</when>
					<when test="shPilcgDelNy==0">AND pilcgDelNy=0</when>
				</choose>
			<if test="shPilcgName != null and !shPilcgName.equals('')"> 
			 AND pilcgName LIKE CONCAT('%',#{shPilcgName},'%')</if> 
				<!-- <if test="shPilcgSeq != null and !shPilcgSeq.equals('')"> 
			 AND pilcgSeq = #{shPilcgSeq}</if> 
 -->	</select>	
	
		<insert id="insert">
			INSERT into pilatesCodeGroup(
			pilcgName
			,pilcgDelNy
			
			) values (
			#{pilcgName}
			,0
			)
	</insert>
	
	<select id="selectOne" resultMap="resultMapObj">
			SELECT 
			pilcgSeq
			, pilcgName
			,pilcgDelNy
			
		FROM nct.pilatesCodeGroup 
		WHERE 1=1
			AND pilcgSeq= #{pilcgSeq}
	</select>
	
	<update id="update">
			UPDATE
				pilatesCodeGroup
			SET 
				pilcgName = #{pilcgName}
			WHERE 1=1
				AND  pilcgSeq = #{pilcgSeq}
	</update>
	
	
	<!-- code s -->
		<!-- code list -->
 		<select id="selectList1" resultMap="resultMapObj">
			SELECT 
			a.pilcdSeq
			, a.pilcdName
			,a.pilcdDelNy
			,pilcgSeq
		FROM nct.pilatesCode a
				WHERE 1=1
			AND pilcdDelNy = 0
			 	<if test="shPilcgSeq != null and !shPilcgSeq.equals('')"> 
			 AND pilcgSeq = #{shPilcgSeq}</if> 
	</select>	
		<insert id="insert1">
			INSERT into pilatesCode(
		
			pilcdName
			,pilcdDelNy
			,pilcgSeq
			) values (
			#{pilcdName}
			,0
			,#{pilcgSeq}
			)
	</insert>
	
	<select id="selectOne1" resultMap="resultMapObj">
			SELECT 
			pilcdSeq
			,pilcdName
			,pilcdDelNy
			,pilcgSeq
			FROM nct.pilatesCode
		WHERE 1=1
			AND pilcdSeq= #{pilcdSeq}
	</select>
	
	<update id="update1">
			UPDATE
				pilatesCode
			SET 
				pilcdName = #{pilcdName}
			WHERE 1=1
				AND  pilcdSeq = #{pilcdSeq}
	</update>
	

</mapper>

